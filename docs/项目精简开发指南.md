# LQRAgent 项目精简开发指南

> **写给谁**：三位团队成员，无论你是否接触过多智能体。
> **怎么用**：按阶段顺序开发，每完成一个任务打勾。前后端可并行。
> **最后更新**：2026-05-23 — 重写为详细开发流程（vibe coding 友好版）

---

## 目录

- [一、项目目标](#一项目目标)
- [二、架构总览（新人必读）](#二架构总览新人必读)
- [三、环境准备与启动](#三环境准备与启动)
- [四、智能体入门（零基础）](#四智能体入门零基础)
- [五、三人分工方案](#五三人分工方案)
- [六、统一事件协议（前后端契约）](#六统一事件协议前后端契约)
- [七、后端开发流程（张三）](#七后端开发流程张三)
- [八、前端开发流程（李四 + 王五）](#八前端开发流程李四--王五)
- [九、阶段验收清单](#九阶段验收清单)
- [十、ai-server 接口说明（我们需要的部分）](#十ai-server-接口说明我们需要的部分)
- [附录 A：Spring Boot 包结构速查](#附录-aspring-boot-包结构速查)
- [附录 B：前端目录结构速查](#附录-b前端目录结构速查)

---

## 一、项目目标

做**面向软件杯 A3 赛题的 AI 课程学习系统**，课程固定为《Python 高级语言程序设计》。

### 赛题 5 项核心要求与本项目对应

| # | 赛题要求 | 我们的实现 |
|---|----------|-----------|
| 1 | 对话式学习画像构建（≥6维度） | `learnerprofile` 智能体 → 前端画像卡片 |
| 2 | 多智能体协同资源生成（≥5种类型） | 讲义 / 题目 / 代码案例 / **示意图** / **短视频** |
| 3 | 个性化学习路径规划与推送 | `learningpath` + 知识图谱 → 前端路径面板 |
| 4 | 智能辅导-多模态答疑（加分项） | `qaagent` + 聊天多模态卡片 |
| 5 | 学习效果评估-动态调整（加分项） | `effectassessment` + 行为追踪 |

### 最小闭环（MVP）

```
学生登录 → 输入"我想学 Python 装饰器" 
→ 系统生成学习路径（路径规划智能体）
→ 点击节点，资源卡片展示讲义/题目/示意图（资源生成智能体）
→ 学生答题，系统更新画像（学生画像智能体）
→ 继续追问，结合知识库回答（答疑智能体）
```

**只要这 6 步跑通，比赛演示就成立。**

---

## 二、架构总览（新人必读）

### 2.1 三层架构

```
┌──────────────────────────────────────────────────────────────┐
│  浏览器 (React :5173)                                         │
│  · 学生工作台 /workspace    · 管理后台 /admin                   │
└──────────────┬───────────────────────────────────────────────┘
               │  HTTP /api/*    WebSocket /ws/*
               ▼
┌──────────────────────────────────────────────────────────────┐
│  后端 BFF (Spring Boot :8080)                                  │
│  · 鉴权 (JWT)                                                 │
│  · 协调编排 (Orchestrator — 判断意图，调用对应智能体)             │
│  · WebSocket 代理 (转发 ai-server 流式输出给前端)                │
│  · 写 MySQL（画像、路径、答题记录、日志）                         │
└──────────────┬───────────────────────────────────────────────┘
               │  HTTP REST     WebSocket
               ▼
┌──────────────────────────────────────────────────────────────┐
│  AI 能力层 DeepTutor (Python :8001)                            │
│  · 大模型调用（对话、生成）                                      │
│  · RAG 知识库检索                                              │
│  · 题目生成、讲义生成                                           │
│  · 代码执行沙箱                                                │
│  【后端自动拉起，不提供 UI】                                     │
└──────────────────────────────────────────────────────────────┘
```

### 2.2 关键设计原则

1. **前端只访问后端**：浏览器 → `:8080`，永远不直连 `:8001`
2. **后端是"中间人"**：鉴权、编排、代理 ai-server、落库，全在后端做
3. **ai-server 是"发动机"**：只提供 AI 能力 API，不碰业务逻辑和 UI
4. **所有模型配置在管理后台配**：启动后浏览器里填 Key，不需要改配置文件

### 2.3 启动后发生了什么

```
1. 启动后端 (mvn spring-boot:run)
   ├─ 自动执行 init-tables.sql 建表
   ├─ 创建测试账号 admin / student1
   ├─ 自动 pip install ai-server 依赖
   ├─ 自动启动 ai-server (PID 由后端管理)
   └─ 后端 API 可用，但 AI 调用暂不可用（还没配 Key）

2. 启动前端 (npm run dev)
   └─ Vite 开发服务器，/api 和 /ws 代理到 :8080

3. 打开浏览器 http://localhost:5173
   ├─ 用 admin / 123456 登录 → 进入管理后台
   ├─ 在「系统配置」填写大模型 API Key
   ├─ 勾选「同步到 ai-server/.env」→ 保存
   ├─ 点击「测试大模型 API」确认连通
   └─ 重启后端（或等 ai-server 热加载配置）

4. 用 student1 / 123456 登录 → 进入学生工作台
   └─ 开始对话学习
```

**这就是你要的一键启动 + 控制台配 Key 流程。ai-server .env 由后端自动写入，你不需要手动改文件。**

---

## 三、环境准备与启动

### 3.1 必需环境

| 工具 | 版本 | 怎么检查 |
|------|------|----------|
| JDK | 21（推荐）或 22-25 | `java -version` |
| Maven | 3.9+ | `mvn -v` |
| Node.js | 18+ / 20 LTS | `node -v` |
| MySQL | 8.0+ | `mysql -u root -p` |
| Python | 3.10+ | `python --version` 或 `python3 --version` |

### 3.2 一次性准备

```sql
-- 1. 创建 MySQL 数据库（用 root 执行）
CREATE DATABASE IF NOT EXISTS agent
    DEFAULT CHARACTER SET utf8mb4
    DEFAULT COLLATE utf8mb4_unicode_ci;
```

```bash
# 2. 修改数据库密码（编辑这一个文件即可）
# backend/src/main/resources/application.properties
# 改这两行：
# spring.datasource.username=root
# spring.datasource.password=你的MySQL密码
```

```bash
# 3. 安装前端依赖（只需一次）
cd frontend
npm install
```

### 3.3 日常启动（每次开发时）

**终端 1 — 启动后端**：
```bash
cd backend
mvn spring-boot:run
# 看到 "Started BackendApplication" 就是成功了
# ai-server 会自动被拉起，日志在 backend/logs/ai-server.log
```

**终端 2 — 启动前端**：
```bash
cd frontend
npm run dev
# 看到 "http://localhost:5173" 就是成功了
```

**浏览器**：打开 `http://localhost:5173`

### 3.4 配置大模型（启动后操作）

1. 浏览器访问 `http://localhost:5173`
2. 用 `admin` / `123456` 登录
3. 左侧导航进入「系统配置」
4. 填写：
   - **大模型提供商**：如 `openai`（兼容 OpenAI 协议的都可以）
   - **模型名称**：如 `gpt-4o-mini`、`deepseek-chat` 等
   - **API Key**：你的 Key
   - **API 地址**：如 `https://api.openai.com/v1`
   - **嵌入模型**：如 `text-embedding-3-large`
5. 勾选「同步到 ai-server/.env」
6. 点击「测试大模型 API」确认连通

> 如果用讯飞星火，选择对应的 provider 并填写星火的 API 地址和 Key。

### 3.5 测试账号

| 用户名 | 密码 | 角色 | 跳转 |
|--------|------|------|------|
| `admin` | `123456` | 管理员 | `/admin` |
| `student1` | `123456` | 学生 | `/workspace` |

---

## 四、智能体入门（零基础）

### 4.1 什么是多智能体？

**一句话**：把一个大任务拆成几个"专家"，每个专家只做自己擅长的事，由一个"协调员"统一调度。

**类比**：就像医院里：
- 挂号处（协调员）判断你该去哪个科室
- 内科医生（答疑智能体）看常见病
- 检验科（内容分析智能体）分析化验单
- 药房（资源生成智能体）配药
- 病历系统（画像智能体）记录你的病史

### 4.2 本项目的 9 个智能体（8 个 specialist + 1 个 orchestrator）

```
用户说："我想学 Python 装饰器"
        │
        ▼
┌──────────────────┐
│  协调智能体        │  ← 判断意图："这是学习路径请求"
│  orchestrator    │
└────────┬─────────┘
         │ 分发任务
         ▼
┌──────────────────┐     ┌─────────────────┐
│  路径规划智能体    │ ──→ │  知识图谱        │
│  learningpath    │     │  (MySQL 里的     │
│                  │     │   知识点依赖关系)  │
└────────┬─────────┘     └─────────────────┘
         │ 生成路径后，用户点击节点
         ▼
┌──────────────────┐
│  资源生成智能体    │  ← 生成讲义/题目/代码/示意图
│  resourcefacade  │
└────────┬─────────┘
         │
    ┌────┴────┐
    ▼         ▼
┌──────┐ ┌──────────┐
│ 文本  │ │ 媒体生成   │  ← 生图/生视频 API
│ ai-  │ │ media-    │
│server│ │ generation│
└──────┘ └──────────┘
         │
         ▼
┌──────────────────┐
│  质量评估智能体     │  ← 校验内容质量，不合格就重试
│  quality-        │
│  assessment      │
└────────┬─────────┘
         │ 推送结果
         ▼
┌──────────────────┐
│  学生画像智能体     │  ← 记录掌握度、易错点
│  learnerprofile  │
└────────┬─────────┘
         │ 反馈调整
         ▼
┌──────────────────┐
│  效果评估智能体     │  ← 分析行为，调整路径
│  effectassessment │
└──────────────────┘

另外两个智能体：
┌──────────────────┐
│  答疑智能体        │  ← 默认对话，学生闲聊或追问
│  qaagent         │
└──────────────────┘
┌──────────────────┐
│  内容分析智能体     │  ← 处理学生上传的 PDF/文档
│  contentanalyzer │
└──────────────────┘
```

### 4.3 智能体之间怎么通信？

在代码层面，智能体 = 一个 Java Service 类。通信方式：

1. **Orchestrator 调用 Specialist**：直接调用 Service 方法
2. **向前端汇报进度**：通过 WebSocket 推送 `agent_step` 事件
3. **共享数据**：都读写 MySQL 的 `learner_profile` 表

**你不需要用什么复杂的框架**（如 LangGraph）。第一版用"if-else 规则路由"就够，P4 再考虑 LLM 意图分类。

---

## 五、三人分工方案

### 5.1 分工表

| 角色 | 代号 | 负责范围 | 关键文件 |
|------|------|----------|----------|
| **后端 + 管理端前端** | 张三 | Spring Boot 所有代码 + 管理后台 UI | `backend/src/main/java/` + `frontend/src/features/admin/` |
| **前端-学生端** | 李四 | 聊天、路径、资源、答题、画像、上传 | `frontend/src/features/workspace/` |
| **前端-公共层** | 王五 | 登录页、公共组件、类型、Store、API 封装、路由 | `frontend/src/components/ui/`、`api/`、`store/`、`types/`、`pages/LoginPage.tsx`、`app/` |

> **张三的管理端前端工作很少**：`features/admin/` 下只有 3 个面板组件（ModelConfigPanel、SystemConfigPanel、UserListPanel）+ AdminPage 壳子，且大部分已有代码。只在 P3 阶段需要新增 Agent 监控面板。

### 5.2 协作约定

1. **API 优先**：张三先定义接口格式 → 王五写好 API 封装 → 李四按格式开发 → 张三实现后联调
2. **WebSocket 协议是核心契约**：见第六章，三人必须对齐
3. **Git 分支**：`backend/dev`、`frontend/dev`，功能完成后合入 `main`
4. **类型共享**：前端 `types/agent-events.ts` 必须与后端 WS 事件 JSON 格式一致
5. **王五先把公共层做好**（登录、路由、Store、类型定义），李四和张三才能高效开发

### 5.3 前后端并行策略

```
Week 1-2:
  张三: P0 WebSocket + AiServerWsProxy + 管理后台 UI 完善
  王五: 登录页、路由守卫、Store 框架、类型定义、HTTP 封装（优先！李四和张三都依赖）
  李四: 等王五完成公共层后开始聊天 UI 对接

Week 3-4:
  张三: P1 知识图谱 + 学习路径 + 资源生成
  李四: 聊天 UI 联调 + 学习路径面板 + 资源展示
  王五: 公共组件打磨 + 协助李四联调

Week 5-6:
  张三: P2 答题 + 画像 + 质量评估
  李四: 答题 UI + 画像卡片 + 多模态卡片
  王五: 整体样式、主题、响应式适配

Week 7-8:
  张三: P3 上传分析 + 效果评估 + Admin Agent 监控面板
  李四: 上传分析结果展示
  王五: 整体联调、演示准备
```

---

## 六、统一事件协议（前后端契约）

> **这是前后端最重要的接口约定。所有 WebSocket 通信必须严格遵循这个格式。**

### 6.1 客户端 → 服务端

```json
{
  "type": "message",
  "content": "我想学 Python 装饰器",
  "session_id": "uuid-可选"
}
```

### 6.2 服务端 → 客户端（7 种事件）

| type | 触发时机 | payload 关键字段 | 前端处理函数 |
|------|----------|-----------------|-------------|
| `chunk` | AI 流式输出每个 token | `content: string` | `appendToLastMessage` |
| `agent_step` | 智能体开始/完成/失败 | `agent, label, status, detail?` | `AgentTimeline` 组件 |
| `artifact` | 智能体产出结构化结果 | `kind, payload` | 根据 kind 分发到路径/资源/媒体面板 |
| `profile_patch` | 画像数据更新 | `payload: partial ProfileSummary` | 头像卡片增量更新 |
| `done` | 本轮对话结束 | `session_id?` | 停止 streaming 动画 |
| `error` | 发生错误 | `content: string` | Toast 提示 + 消息区显示 |

### 6.3 `agent_step` 状态

```json
// 智能体开始工作
{ "type": "agent_step", "agent": "path_planner", "label": "正在规划学习路径...", "status": "running" }

// 智能体完成
{ "type": "agent_step", "agent": "path_planner", "label": "学习路径已生成", "status": "done" }

// 智能体失败
{ "type": "agent_step", "agent": "path_planner", "label": "路径生成失败", "status": "failed", "detail": "知识图谱未加载" }
```

### 6.4 `artifact.kind` 全量类型

| kind | payload 结构 | 前端目标组件 |
|------|-------------|-------------|
| `learning_path` | `{ goal, nodes[], planDescription }` | `PathSection` |
| `lesson` | `{ kpId, title, content (Markdown) }` | `ResourceSection` text tab |
| `quiz` | `{ kpId, questions[{stem, options[], answer}] }` | `QuizSection` |
| `code_case` | `{ kpId, title, code, explanation }` | `ResourceSection` code tab |
| `media_image` | `{ url, mime, title, kpId, thumbnailUrl? }` | `MediaPreview` / image tab |
| `media_video` | `{ url, mime, title, kpId, durationSec?, posterUrl? }` | `MediaPreview` / video tab |
| `multi_card` | `[{type:"text"|"image"|"video", ...}]` | `MultiCardMessage` 聊天内嵌卡片 |
| `profile_patch` | `Partial<ProfileSummary>` | `ProfileCard` 增量更新 |

### 6.5 智能体标识枚举

```java
// 后端 agent 字段只能是以下值之一：
public enum AgentId {
    orchestrator,      // 协调
    content_analyzer,  // 内容分析
    path_planner,      // 路径规划
    resource_facade,   // 资源生成
    quality_assessment,// 质量评估
    learner_profile,   // 学生画像
    qa_agent,          // 答疑
    effect_assessment  // 效果评估
}
```

前端对应 `types/agent-events.ts` 里的 `AgentId` 类型，两边必须一致。

---

## 七、后端开发流程（张三）

> **你的目标**：实现所有 REST API + WebSocket + 9 个智能体模块。

### P0 — 聊天打通（预计 5-7 天）

**阶段目标**：`student1` 在工作台发消息能流式显示回复，Timeline 显示智能体步骤。

---

#### 任务 P0-1：ChatWebSocketHandler（/ws/chat 入口）

**目标**：实现 WebSocket 端点，前端能连上并收发消息。

**新建文件**：`backend/src/main/java/com/lqragent/backend/chat/ChatWebSocketHandler.java`

**实现步骤**：
1. 实现 `WebSocketHandler` 接口
2. `afterConnectionEstablished`：从 URL query 中取 `token`，用 `JwtUtil` 校验，校验失败直接 `session.close()`
3. `handleTextMessage`：解析 JSON 得到 `type` + `content` + `session_id`
4. 将消息转发给 `OrchestratorService.process()`（P0 先直接调 `QaAgentService`）
5. `afterConnectionClosed`：清理资源

**关键代码框架**：
```java
@Component
public class ChatWebSocketHandler extends TextWebSocketHandler {
    
    private final JwtUtil jwtUtil;
    private final OrchestratorService orchestrator; // P0 先直接调 QaAgentService
    
    // 用 ConcurrentHashMap 管理所有在线连接
    private final ConcurrentHashMap<String, WebSocketSession> sessions = new ConcurrentHashMap<>();
    
    @Override
    public void afterConnectionEstablished(WebSocketSession session) {
        // 1. 从 URI 取 token
        String token = extractToken(session.getUri());
        // 2. 校验 JWT，取 userId
        String userId = jwtUtil.extractUserId(token);
        // 3. 存入 sessions map
        sessions.put(userId, session);
    }
    
    @Override
    protected void handleTextMessage(WebSocketSession session, TextMessage message) {
        // 1. 解析 JSON
        // 2. 调 orchestrator.process(userId, content, sessionId)
        // 3. orchestrator 内部通过 session.sendMessage() 推送事件
    }
}
```

**涉及已有文件**：`SecurityConfig.java`（已放行 `/ws/**`，无需改）、`JwtUtil.java`（已有校验方法）

**验收**：postman 或浏览器 console 能建立 WS 连接，不会 4xx/5xx。

---

#### 任务 P0-2：AiServerWsProxy（转发 ai-server 流式输出）

**目标**：后端作为中间人连接 ai-server 的 WebSocket，把 ai-server 的流式事件转换成我们的协议格式。

**新建文件**：`backend/src/main/java/com/lqragent/backend/chat/AiServerWsProxy.java`

**实现步骤**：
1. 使用 Java 11 `HttpClient` 或 Spring `WebSocketClient` 连接 ai-server 的 `/api/v1/ws`
2. 收到 ai-server 的流式文本 → 包装为 `{"type":"chunk","content":"..."}` → 发给前端
3. ai-server 结束 → 发 `{"type":"done"}` 给前端
4. ai-server 报错 → 发 `{"type":"error","content":"..."}` 给前端

**ai-server WebSocket 地址**：`ws://localhost:8001/api/v1/ws`（从 `sys_config` 读取 base-url 拼接）

**关键代码框架**：
```java
@Component
public class AiServerWsProxy {
    
    private final AppRuntimeConfig runtimeConfig;
    
    public void proxy(String userMessage, String sessionId, 
                      WebSocketSession frontendSession) {
        // 1. 连接 ai-server WS
        String aiWsUrl = runtimeConfig.getAiServerBaseUrl()
                .replace("http://", "ws://")
                .replace("https://", "wss://") + "/api/v1/ws";
        
        // 2. 发送用户消息给 ai-server
        // 3. 循环接收 ai-server 事件
        // 4. 转换格式后通过 frontendSession.sendMessage() 发给前端
        // 5. 完成后关闭 ai-server 连接
    }
}
```

**验收**：前端发消息 → ai-server 有响应 → 前端收到 chunk 事件 → 流式显示文本。

---

#### 任务 P0-3：QaAgentService（默认答疑通道）

**目标**：最简单的答疑智能体，P0 阶段不走 orchestrator 路由，所有消息直接调用 ai-server 对话。

**新建文件**：`backend/src/main/java/com/lqragent/backend/qaagent/QaAgentService.java`

**实现步骤**：
1. 注入 `AiServerWsProxy`
2. 一个 `answer(userId, message, sessionId, frontendSession)` 方法
3. 调 proxy 转发到 ai-server
4. P0 阶段先不写 `agent_run_log`（P0-4 再补）

**验收**：学生发 "你好" → 前端显示 ai-server 的回复。

---

#### 任务 P0-4：AgentRunLogService + agent_step 事件

**目标**：每次智能体调用都记录到数据库，并向前端推送步骤条事件。

**新建文件**：
- `backend/src/main/java/com/lqragent/backend/observability/AgentRunLogService.java`
- `backend/src/main/java/com/lqragent/backend/observability/entity/AgentRunLog.java`（JPA Entity）
- `backend/src/main/java/com/lqragent/backend/observability/repository/AgentRunLogRepository.java`

**实现步骤**：
1. `AgentRunLog` Entity 映射 `agent_run_log` 表（表已在 SQL 中定义）
2. `AgentRunLogService` 提供三个方法：
   - `start(userId, sessionId, agent, intent)` → 插入一条 RUNNING 记录，推 `agent_step(status:running)`
   - `complete(runId)` → 更新为 SUCCESS + 计算耗时，推 `agent_step(status:done)`
   - `fail(runId, error)` → 更新为 FAILED + 错误信息，推 `agent_step(status:failed)`
3. 推事件方法：通过 `WebSocketSession.sendMessage(new TextMessage(json))`

**验收**：发一条消息 → 前端 AgentTimeline 至少显示一个步骤条（如 "答疑智能体" running → done）。

---

#### 任务 P0-5：ChatSessionService（会话管理）

**目标**：管理每次对话的 session，支持前端刷新后恢复。

**新建文件**：
- `backend/src/main/java/com/lqragent/backend/chat/ChatSessionService.java`
- `backend/src/main/java/com/lqragent/backend/chat/entity/ChatSession.java`（JPA Entity）
- `backend/src/main/java/com/lqragent/backend/chat/repository/ChatSessionRepository.java`

**实现步骤**：
1. Entity 映射 `chat_session` 表
2. 方法：
   - `getOrCreate(userId)`：如果没有活跃 session 就创建
   - `endSession(sessionId)`：标记结束

**验收**：同一学生多次发消息，session_id 保持一致。

---

### P1 — 路径与资源（预计 7-10 天）

**阶段目标**：输入目标后出现真实学习路径，点击节点能生成讲义/题目/示意图。

---

#### 任务 P1-1：填写 knowledge_graph.json + KnowledgeGraphService

**目标**：把《Python 高级语言程序设计》的知识点依赖关系整理成 JSON，加载到 MySQL。

**修改文件**：`backend/src/main/resources/course/knowledge_graph.json`

**JSON 格式**（至少填 5-6 章）：
```json
{
  "course_id": "python_course",
  "course_name": "高级语言程序设计Python",
  "chapters": [
    {
      "chapter_id": "ch01",
      "title": "Python基础语法",
      "knowledge_points": [
        {
          "kp_id": "kp_input_output",
          "name": "输入输出",
          "prerequisites": [],
          "difficulty": 1,
          "resource_tags": ["lesson", "quiz"]
        },
        {
          "kp_id": "kp_variables",
          "name": "变量与数据类型",
          "prerequisites": ["kp_input_output"],
          "difficulty": 1,
          "resource_tags": ["lesson", "code", "quiz"]
        }
      ]
    }
  ]
}
```

**建议章节**（6 章 ≈ 30 个知识点）：
1. Python 基础语法（输入输出、变量、运算符、字符串）
2. 流程控制（if/else、for、while、break/continue）
3. 函数（定义、参数、返回值、lambda、作用域）
4. 数据结构（list、tuple、dict、set、推导式）
5. 面向对象（类、继承、多态、装饰器、property）
6. 文件与异常（文件读写、with、try/except、自定义异常）

**新建文件**：`backend/src/main/java/com/lqragent/backend/knowledgegraph/KnowledgeGraphService.java`

**实现步骤**：
1. 启动时把 JSON 解析并 INSERT 到 `knowledge_point` 和 `knowledge_edge` 表
2. 提供三个查询方法：
   - `getPrerequisites(kpId)` → 返回前置知识点列表
   - `findPath(fromKpId, toKpId)` → BFS/DFS 找最短路径
   - `getAllPoints()` → 返回全部知识点

**验收**：调用 API 能查到知识点的前置依赖关系。

---

#### 任务 P1-2：LearningPathService 真逻辑

**目标**：替换占位数据，根据目标关键词匹配知识点，生成真实路径。

**修改文件**：`backend/src/main/java/com/lqragent/backend/learningpath/service/LearningPathService.java`

**实现步骤**：
1. 接收 `goal` 参数（如 "学习 Python 装饰器"）
2. 在知识图谱中模糊匹配目标知识点
3. 从根节点到目标节点 BFS 计算最短路径
4. 可选：调 ai-server 生成 `planDescription`（自然语言学习计划，LLM 润色）
5. 结果写入 `learning_path` 和 `learning_path_step` 表
6. 通过 WS 推 `artifact(kind=learning_path)` 给前端

**验收**：输入 "学习 Python 装饰器" → API 返回至少 3 个节点的路径（如：函数 → 闭包 → 装饰器），前端不再显示 `kp_placeholder_1`。

---

#### 任务 P1-3：OrchestratorService 规则路由

**目标**：协调智能体，根据用户消息判断意图，调度对应模块。

**新建文件**：`backend/src/main/java/com/lqragent/backend/orchestrator/OrchestratorService.java`

**路由规则（关键词匹配）**：
```java
public AgentIntent classify(String message) {
    if (containsAny(message, "路径", "计划", "学什么", "学习路线")) 
        return AgentIntent.LEARN_PATH;
    if (containsAny(message, "出题", "练习", "测验", "做题")) 
        return AgentIntent.GENERATE_QUIZ;
    if (containsAny(message, "讲义", "总结", "思维导图", "讲解")) 
        return AgentIntent.GENERATE_LESSON;
    if (containsAny(message, "示意图", "配图", "画个图")) 
        return AgentIntent.GENERATE_IMAGE;
    if (containsAny(message, "视频", "动画", "演示")) 
        return AgentIntent.GENERATE_VIDEO;
    return AgentIntent.QA; // 默认走答疑
}
```

**实现步骤**：
1. `process(userId, message, sessionId, frontendSession)` 方法
2. 先 `classify(message)` 判意图
3. 调 `AgentRunLogService.start()` 记录开始
4. 按意图调用对应 Service
5. 推送 `agent_step`、`chunk`、`artifact` 事件
6. 调 `AgentRunLogService.complete()` 记录结束

**修改已有文件**：`ChatWebSocketHandler.handleTextMessage()` — 改为调 `orchestrator.process()` 而不是直接调 `QaAgentService`。

**验收**：说 "给我规划学习Python装饰器的路径" → orchestrator 识别意图 → path_planner 生成路径 → 前端收到 `learning_path` artifact。

---

#### 任务 P1-4：ResourceFacadeService + resources/generate API

**目标**：点击路径节点后能生成对应资源（讲义/题目/代码）。

**新建文件**：
- `backend/src/main/java/com/lqragent/backend/resourcefacade/ResourceFacadeService.java`
- `backend/src/main/java/com/lqragent/backend/resourcefacade/controller/ResourceController.java`

**实现步骤**：
1. `POST /api/resources/generate` 接口：
   ```json
   { "kpId": "kp_decorator", "type": "LESSON", "prompt": "可选" }
   ```
2. `type=LESSON` → 调 `AiServerClient.generateLesson(kbName, kpId)` → 返回 Markdown
3. `type=QUIZ` → 调 `AiServerClient.generateQuestion(kbName, topic, count=5)`
4. `type=CODE_CASE` → 调 ai-server 生成代码案例
5. `type=ILLUSTRATION` → 调 `MediaGenerationService`（见 P1-5）
6. 结果写入 `resource_item` 表
7. 通过 WS 推 `artifact(kind=lesson|quiz|code_case|media_image)` 给前端

**验收**：点击路径节点 → 生成对应资源 → 前端资源 Tab 显示内容。

---

#### 任务 P1-5：MediaGenerationService（生图）

**目标**：给知识点生成示意图。

**新建文件**：`backend/src/main/java/com/lqragent/backend/mediageneration/MediaGenerationService.java`

**实现步骤**：
1. 根据 kpId 查询知识点标题和描述
2. 拼一个绘图 prompt（可调 LLM 把中文描述转英文 prompt）
3. 调生图 API（讯飞星火图片生成、DashScope 万相、或 OpenAI DALL-E）
4. 下载图片保存到 `backend/storage/media/{userId}/{uuid}.png`
5. 写 `resource_item` 表（`resource_type=ILLUSTRATION`，`media_url` 指向 `/api/media/{id}`）
6. 通过 WS 推 `artifact(kind=media_image)`

**生图 API 配置**：管理后台 `sys_config` 增加 `media.image.provider`、`media.image.api-key`、`media.image.model` 等键，与 LLM 配置并列。

**验收**：点节点 → 返回一张示意图 → 前端图片 Tab 展示。

---

### P2 — 答题与画像（预计 5-7 天）

**阶段目标**：做完题能更新掌握情况，左侧画像有变化。

---

#### 任务 P2-1：LearnerProfileService + 画像 API

**新建文件**：
- `backend/src/main/java/com/lqragent/backend/learnerprofile/LearnerProfileService.java`
- `backend/src/main/java/com/lqragent/backend/learnerprofile/entity/LearnerProfile.java`
- `backend/src/main/java/com/lqragent/backend/learnerprofile/repository/LearnerProfileRepository.java`

**实现步骤**：
1. Entity 映射 `learner_profile` 表
2. `GET /api/profile/summary` → 返回学生画像摘要（至少 6 个维度）：
   - 知识基础（已掌握知识点数/总数）
   - 学习进度（路径完成百分比）
   - 认知风格（如 "偏好视频" "偏好文字"）
   - 易错知识点（错题最多的 3 个知识点）
   - 学习活跃度（本周学习次数）
   - 推荐下一步
3. `patchProfile(userId, updates)` → 被其他智能体调用，更新字段
4. 通过 WS 推 `profile_patch` 给前端（增量更新，不需要刷新页面）

**验收**：`GET /api/profile/summary` 返回 6 个维度数据。

---

#### 任务 P2-2：QuizController + 答题 API

**新建文件**：
- `backend/src/main/java/com/lqragent/backend/qaagent/controller/QuizController.java`（或单独 quiz 包）
- `backend/src/main/java/com/lqragent/backend/qaagent/entity/QuizRecord.java`
- `backend/src/main/java/com/lqragent/backend/qaagent/repository/QuizRecordRepository.java`

**实现步骤**：
1. `POST /api/quiz/submit`：
   ```json
   { "kpId": "kp_decorator", "answers": [{"questionId": 1, "answer": "B"}] }
   ```
2. 判分逻辑：对比正确答案（题目生成时已存 `resource_item` 的 `content` JSON 中）
3. 写入 `quiz_record` 表
4. 触发 `LearnerProfileService.patchProfile()` 更新掌握度
5. 触发 `EffectAssessmentService.analyze()` 看是否需要调整路径
6. 返回得分 + 解析

**验收**：答一道题 → 返回正确/错误 + 解析 → 画像数据变化。

---

#### 任务 P2-3：QualityAssessmentService（质检钩子）

**目标**：资源生成后做基本校验，不合格的重试一次。

**新建文件**：`backend/src/main/java/com/lqragent/backend/qualityassessment/QualityAssessmentService.java`

**校验内容**：
1. 文本类：检查内容不为空、Markdown 格式正常、包含实际教学内容
2. 图片类：检查文件大小 > 0、尺寸合理（不算太小）
3. 视频类（P4）：检查时长在 15-60s 范围、无敏感内容

**验收**：故意生成失败 → 自动重试一次 → `agent_run_log` 有质检步骤记录。

---

### P3 — 上传分析 + 管理监控（预计 5-7 天）

**阶段目标**：上传的资料真正进入知识库；管理员能看智能体运行统计。

---

#### 任务 P3-1：uploadDocument 实现

**修改文件**：`AiServerClient.java` — 补全 `uploadDocument` 方法

**实现步骤**：
1. 读文件内容
2. 调 ai-server `/api/v1/knowledge/{kb_id}/documents` 上传
3. 返回文档 ID

---

#### 任务 P3-2：ContentAnalyzerService

**新建文件**：`backend/src/main/java/com/lqragent/backend/contentanalyzer/ContentAnalyzerService.java`

**实现步骤**：
1. 修改 `UploadQueueService.processTask()`：
   - 上传到 ai-server 知识库（调 P3-1）
   - 调 ContentAnalyzer 分析文档内容
2. ContentAnalyzer：
   - 用 LLM 提取文档涉及的知识点
   - 写入 `kb_upload_task.analysis_result` 和 `mapped_kp_ids`

**验收**：上传一篇 Python 教程 PDF → 队列显示 COMPLETED → 能看到映射了哪些知识点。

---

#### 任务 P3-3：Admin Agent 统计 API

**新建文件**：在 `AdminController` 和 `AdminService` 中增加方法

**实现步骤**：
1. `GET /api/admin/agent-stats?days=7` → 各 agent 调用次数、成功率、平均耗时
2. `GET /api/admin/agent-runs?limit=50` → 分页查最近 `agent_run_log`

**验收**：管理后台智能体监控 Tab 能看到统计数据和日志列表。

---

### P4 — 加分项（时间允许再做）

- **生视频**（`VIDEO_CLIP`）：MediaGenerationService 接生视频 API
- **LLM 意图分类**：用大模型替代关键词规则判断意图
- **路径动态调整**：根据错题自动插入复习节点
- **chat_message 全量持久化**：含多模态消息历史回放
- **管理端媒体 API 配置**：Admin UI 配置生图/生视频 provider

---

## 八、前端开发流程（李四 + 王五）

### 王五：公共层 + 管理后台（P0 优先）

---

#### 任务 W0-1：类型定义（与后端协议对齐）

**目标**：把第六章的事件协议翻译成 TypeScript 类型。

**已有文件**（在 `frontend/src/types/` 下，需与后端对齐）：
- `agent-events.ts` — WS 消息类型、AgentId 枚举
- `artifact.ts` — artifact kind 和 payload 类型
- `chat.ts` — 消息类型
- `multi-card.ts` — 多模态卡片块类型
- `media-resource.ts` — 媒体资源响应
- `learning-path.ts` — 路径节点类型
- `profile.ts` — 画像类型

**验收**：所有类型与文档第六章一致，编译无报错。

---

#### 任务 W0-2：HTTP 封装 + Store 框架

**已有文件**（检查并完善）：
- `api/http.ts` — axios 实例，自动带 token，401 跳登录（✅ 已有）
- `api/auth.ts` — login/logout（✅ 已有）
- `api/admin.ts` — 管理后台 API（✅ 已有，P3 增加 agent-stats/agent-runs）
- `store/authStore.ts` — 登录状态（✅ 已有）
- `store/chatStore.ts` — 消息列表、sessionId、streaming 状态（✅ 已有，待 WS 联调）
- `store/agentTraceStore.ts` — 智能体步骤列表（✅ 已有）
- `store/pathStore.ts` — 学习路径（✅ 已有）
- `store/artifactStore.ts` — artifact 缓存（✅ 已有）
- `store/profileStore.ts` — 画像状态（✅ 已有）

**验收**：登录流程跑通，admin 登录能进 `/admin`，student1 登录进 `/workspace`。

---

#### 任务 W0-3：登录页 + 路由守卫

**已有**：`pages/LoginPage.tsx`（✅）、`app/ProtectedRoute.tsx`（✅）

**验收**：未登录访问 `/workspace` → 自动跳 `/login`。登录后按角色跳 `/workspace` 或 `/admin`。

---

#### 任务 W0-4：管理后台 UI

**已有文件**（在 `features/admin/` 下，完善即可）：
- `ModelConfigPanel.tsx` — 大模型配置表单（✅ 已有）
- `SystemConfigPanel.tsx` — 系统配置管理（✅ 已有）
- `UserListPanel.tsx` — 用户列表（✅ 已有）
- `AdminPage.tsx` — 管理后台壳子（✅ 已有）

**需要增加**：
- 管理后台需要增加重启 ai-server 的按钮（调用后端 API 触发重启）
- P3 阶段增加智能体监控面板（调 `/api/admin/agent-stats`、`/api/admin/agent-runs`）

**验收**：admin 登录后能配置大模型 Key，能测试连通性，能查看用户列表和上传队列。

---

### 李四：学生工作台（P0-P2 逐步推进）

---

#### 任务 L0-1：工作台壳子 + 聊天 UI（P0，等王五公共层完成）

**已有文件**（在 `features/workspace/` 下）：
- `shell/WorkspaceShell.tsx` — 左右两栏布局（✅）
- `shell/WorkspaceSidebar.tsx` — 左侧导航（✅）
- `chat/ChatView.tsx` — 聊天主视图，装配 ChatPanel + Timeline + 辅区（✅）
- `chat/ChatPanel.tsx` — 消息列表 + 输入框 + 发送（❌ 看实际文件）
- `chat/ChatMessageList.tsx` — 消息列表（✅）
- `chat/ChatComposer.tsx` — 输入框（✅）
- `chat/StreamingMessage.tsx` — 流式 Markdown 渲染（✅）
- `chat/AgentTimeline.tsx` — 智能体步骤条（✅）
- `chat/MultiCardMessage.tsx` — 多模态卡片消息（✅）

**需要完善**：
1. `ChatComposer` 发送按钮 → 调 `useWebSocket().send(content)`
2. 确认 `useWebSocket.ts` 连接逻辑正确（当前 `connect()` 在 `useEffect` 中自动调）
3. 测试：后端 P0 完成后，前端能显示流式回复

**验收**：输入 "你好" → 发送 → 看到 AI 逐字回复 → AgentTimeline 显示步骤。

---

#### 任务 L1-1：学习路径面板（P1）

**已有文件**：
- `context/PathSection.tsx` — 路径列表展示（❌ 确认是否已实现）
- `context/MainTabBar.tsx` — Tab 切换栏（✅）

**需要完善**：
1. 学生输入学习目标 → 发送 WS 消息 → 后端推 `artifact(kind=learning_path)` → `pathStore` 更新 → `PathSection` 展示节点列表
2. 点击节点 → 高亮当前节点 → 触发资源生成
3. 刷新后通过 `GET /api/learning-path/current` 恢复路径（P1 后端也要实现）

**验收**：说 "我想学 Python 装饰器" → 路径面板出现节点列表 → 点击节点能触发资源生成。

---

#### 任务 L1-2：资源展示区（P1）

**已有文件**：
- `context/ResourceSection.tsx` — 资源 Tab 面板（❌ 确认实现状态）
- `context/MediaPreview.tsx` — 图片/视频预览（❌ 确认实现状态）
- `context/QuizSection.tsx` — 答题区（❌ 确认实现状态）

**需要实现**：
1. 收到 `artifact(kind=lesson)` → 文本 Tab 展示 Markdown 讲义
2. 收到 `artifact(kind=code_case)` → 代码 Tab 展示（带语法高亮）
3. 收到 `artifact(kind=media_image)` → 示意图 Tab 展示 `<img>`
4. `MediaPreview` 组件：点击缩略图放大、支持旋转/下载

**验收**：点路径节点 → 资源面板显示对应类型的内容。

---

#### 任务 L2-1：答题 UI（P2）

**已有文件**：`context/QuizSection.tsx`

**需要实现**：
1. 展示题目列表（选择题/判断题为主）
2. 学生选择答案 → 调 `POST /api/quiz/submit`
3. 显示得分、正确答案、解析
4. 提交后自动更新画像（通过 `profile_patch` 事件或触发 GET summary）

**验收**：做完一道题 → 看到对错 → 画像数字有变化。

---

#### 任务 L2-2：画像展示（P2）

**已有文件**：
- `profile/ProfileCard.tsx` — 左侧导航里的画像摘要卡片（✅）
- `profile/ProfileDetailView.tsx` — `/workspace/profile` 详情页（❌ 确认实现状态）

**需要实现**：
1. `ProfileCard` 消费 `profileStore`，实时显示画像摘要
2. 页面加载时调 `GET /api/profile/summary` 初始化
3. WS 收到 `profile_patch` 时增量更新
4. `ProfileDetailView`：完整的画像详情页（/workspace/profile 路由）

**验收**：左侧卡片显示 6 个维度的数据，答题后数字自动变化。

---

#### 任务 L3-1：上传分析结果（P3）

**已有文件**：
- `upload/UploadQueuePanel.tsx` — 上传队列（✅）
- `upload/UploadAnalysisCard.tsx` — 分析结果卡片（❌ 确认实现状态）

**需要实现**：
1. 上传文件后轮询队列状态
2. COMPLETED 后展示 `mapped_kp_ids` 和 `analysis_result`
3. 点击知识点可跳转到路径中对应节点

**验收**：上传 PDF → 等待处理 → 看到映射的知识点列表。

---

## 九、阶段验收清单

### P0 验收（聊天打通）
- [ ] `student1` 登录进入工作台
- [ ] 发送消息后能看到 AI 逐字回复（流式输出）
- [ ] AgentTimeline 显示至少 1 个智能体步骤
- [ ] 数据库 `agent_run_log` 表有记录
- [ ] 不再显示红色「连接中…」

### P1 验收（路径 + 资源）
- [ ] 输入 "学习 Python 装饰器" → 出现 3+ 节点的真实路径
- [ ] 点击节点 → 出现讲义/题目/代码（至少一种）
- [ ] 至少一个知识点有示意图（生图）
- [ ] 路径刷新后能恢复

### P2 验收（答题 + 画像）
- [ ] 能做一道题并看到对错
- [ ] 左侧画像至少有 6 个维度数据
- [ ] 答题后画像数字变化
- [ ] 聊天消息里能内嵌图片卡片（MULTI_CARD）

### P3 验收（上传 + 监控）
- [ ] 上传 Python 教程 PDF → 队列完成 → 看到映射知识点
- [ ] 管理后台 Agent 监控 Tab 能看到统计

### P4 验收（加分项）
- [ ] 至少一个知识点有短视频
- [ ] LLM 意图分类准确率 > 80%
- [ ] 全量消息历史回放

---

## 十、ai-server 接口说明（我们需要的部分）

> **DeepTutor 开源项目提供的接口很多，我们只用到以下这些。其他的不用管。**

### 10.1 我们使用的接口

| 用途 | 方法 | 路径 | 我们哪里调 |
|------|------|------|-----------|
| 健康检查 | GET | `/api/v1/knowledge/health` | `AiServerClient.ping()` ✅ 已实现 |
| 创建知识库 | POST | `/api/v1/knowledge/kb` | `AiServerClient.createKnowledgeBase()` ✅ 已实现 |
| 上传文档 | POST | `/api/v1/knowledge/{kb_id}/documents` | `AiServerClient.uploadDocument()` ❌ 待实现 (P3) |
| 生成题目 | POST | `/api/v1/question/generate` | `AiServerClient.generateQuestion()` 🟡 HTTP 有，未串联 |
| 生成讲义 | POST | `/api/v1/book/generate` | `AiServerClient.generateLesson()` 🟡 HTTP 有，未串联 |
| 流式对话 | WS | `/api/v1/ws` | `AiServerWsProxy` ❌ 待实现 (P0) |
| 深度求解 | WS | `/api/v1/ws`（传 capability） | 同上，通过 WS 协议区分 |

### 10.2 我们不用的接口

- ai-server 的 Next.js Web UI（`ai-server/web/`）— 我们自研前端
- CLI 模式（`deeptutor chat`）— 我们只走 API/WS
- Matrix/即时通讯集成（TutorBot）— 比赛不涉及
- GeoGebra 数学可视化 — 课程是 Python，不需要
- 论文搜索（arXiv）— 不需要
- 协作写作、图书编辑器 — 不需要

### 10.3 ai-server .env 配置（后端自动管理）

我们对 ai-server 的 `.env` 文件是**写操作**（由 `AiServerEnvSyncService` 自动同步），不需要手动编辑。

ai-server 启动需要的核心环境变量（我们会自动写入）：

```
LLM_BINDING=openai
LLM_MODEL=gpt-4o-mini
LLM_API_KEY=sk-xxx
LLM_HOST=https://api.openai.com/v1
EMBEDDING_BINDING=openai
EMBEDDING_MODEL=text-embedding-3-large
EMBEDDING_API_KEY=sk-xxx
EMBEDDING_HOST=https://api.openai.com/v1/embeddings
```

---

## 附录 A：Spring Boot 包结构速查

```
com.lqragent.backend
├── auth/                        # 认证：登录/登出 ✅
│   ├── controller/AuthController.java
│   ├── service/AuthService.java
│   └── dto/LoginRequest.java, LoginResponse.java
├── user/                        # 用户信息 ✅
│   ├── controller/UserController.java
│   ├── service/UserService.java, CurrentUserService.java
│   ├── entity/User.java
│   └── repository/UserRepository.java
├── security/                    # JWT + Spring Security ✅
│   ├── JwtUtil.java
│   ├── JwtAuthFilter.java
│   └── SecurityConfig.java
├── admin/                       # 管理后台 ✅
│   ├── controller/AdminController.java
│   ├── service/AdminService.java, ModelConfigService.java
│   └── dto/
├── systemconfig/                # 运行时配置 ✅
│   ├── service/SysConfigService.java
│   ├── entity/SysConfig.java
│   ├── repository/SysConfigRepository.java
│   ├── AppRuntimeConfig.java
│   ├── ConfigKeys.java
│   └── SysConfigSeeder.java
├── aiserver/                    # AI Server 客户端
│   ├── AiServerClient.java     # HTTP 调用封装 🟡
│   └── AiServerEnvSyncService.java # .env 同步 ✅
├── uploadqueue/                 # 上传队列
│   ├── controller/UploadQueueController.java ✅
│   ├── service/UploadQueueService.java 🟡
│   ├── entity/KbUploadTask.java ✅
│   └── repository/KbUploadTaskRepository.java ✅
├── config/                      # 启动配置
│   ├── AIServerRunner.java     # 自动拉起 ai-server ✅
│   ├── DataInitializer.java    # 测试账号初始化 ✅
│   └── SchedulingConfig.java   # 定时任务 ✅
├── common/                      # 公共
│   ├── dto/ApiResponse.java
│   └── exception/GlobalExceptionHandler.java, BusinessException.java
│
│   👇 以下全部待新建 👇
│
├── chat/                        # WebSocket 聊天 (P0)
│   ├── ChatWebSocketHandler.java          ❌ P0-1
│   ├── AiServerWsProxy.java               ❌ P0-2
│   ├── ChatSessionService.java            ❌ P0-5
│   ├── entity/ChatSession.java            ❌ P0-5
│   └── repository/ChatSessionRepository.java ❌ P0-5
├── orchestrator/                # 协调智能体 (P1)
│   └── OrchestratorService.java           ❌ P1-3
├── knowledgegraph/              # 知识图谱 (P1)
│   ├── KnowledgeGraphService.java         ❌ P1-1
│   ├── entity/KnowledgePoint.java, KnowledgeEdge.java  ❌ P1-1
│   └── repository/                       ❌ P1-1
├── learningpath/                # 学习路径 (P1)
│   ├── controller/LearningPathController.java 🟡 已有占位
│   ├── service/LearningPathService.java   ❌ P1-2 真逻辑
│   ├── entity/LearningPath.java, LearningPathStep.java ❌
│   └── repository/                       ❌
├── resourcefacade/              # 资源生成 (P1)
│   ├── ResourceFacadeService.java         ❌ P1-4
│   └── controller/ResourceController.java ❌ P1-4
├── mediageneration/             # 媒体生成-生图/生视频 (P1/P4)
│   └── MediaGenerationService.java        ❌ P1-5
├── qaagent/                     # 答疑智能体 (P0)
│   ├── QaAgentService.java                ❌ P0-3
│   └── controller/QuizController.java     ❌ P2-2
├── learnerprofile/              # 学生画像 (P2)
│   ├── LearnerProfileService.java         ❌ P2-1
│   ├── entity/LearnerProfile.java         ❌ P2-1
│   └── repository/                        ❌ P2-1
├── qualityassessment/           # 质量评估 (P2)
│   └── QualityAssessmentService.java      ❌ P2-3
├── contentanalyzer/             # 内容分析 (P3)
│   └── ContentAnalyzerService.java        ❌ P3-2
├── effectassessment/            # 效果评估 (P3)
│   └── EffectAssessmentService.java       ❌ P3
├── observability/               # 可观测性 (P0)
│   ├── AgentRunLogService.java            ❌ P0-4
│   ├── entity/AgentRunLog.java            ❌ P0-4
│   └── repository/AgentRunLogRepository.java ❌ P0-4
└── console/                     # 控制台(可选)
    └── ConsoleControlPanel.java           ✅ 已有
```

---

## 附录 B：前端目录结构速查

```
frontend/src/
├── app/                              # 应用壳
│   ├── App.tsx                        ✅
│   ├── ProtectedRoute.tsx             ✅
│   └── index.ts                       ✅
├── pages/                            # 路由页面
│   ├── LoginPage.tsx                  ✅ 王五
│   ├── WorkspacePage.tsx              ✅ 李四
│   └── AdminPage.tsx                  ✅ 王五
├── features/                         # 业务功能
│   ├── workspace/                    # 学生工作台（李四）
│   │   ├── shell/
│   │   │   ├── WorkspaceShell.tsx     ✅
│   │   │   └── WorkspaceSidebar.tsx   ✅
│   │   ├── chat/
│   │   │   ├── ChatView.tsx           ✅
│   │   │   ├── ChatPanel.tsx          ✅
│   │   │   ├── ChatMessageList.tsx    ✅
│   │   │   ├── ChatComposer.tsx       ✅
│   │   │   ├── StreamingMessage.tsx   ✅
│   │   │   ├── AgentTimeline.tsx      ✅
│   │   │   └── MultiCardMessage.tsx   ✅
│   │   ├── context/
│   │   │   ├── MainTabBar.tsx         ✅
│   │   │   ├── PathSection.tsx        🟡 李四 P1
│   │   │   ├── ResourceSection.tsx    🟡 李四 P1
│   │   │   ├── MediaPreview.tsx       🟡 李四 P1
│   │   │   └── QuizSection.tsx        🟡 李四 P2
│   │   ├── upload/
│   │   │   ├── UploadQueuePanel.tsx   ✅
│   │   │   └── UploadAnalysisCard.tsx 🟡 李四 P3
│   │   └── profile/
│   │       ├── ProfileCard.tsx        ✅
│   │       └── ProfileDetailView.tsx  🟡 李四 P2
│   └── admin/                        # 管理后台（王五）
│       ├── ModelConfigPanel.tsx       ✅
│       ├── SystemConfigPanel.tsx      ✅
│       └── UserListPanel.tsx          ✅
├── components/ui/                    # 通用 UI（王五）
│   ├── Badge/                         ✅
│   ├── EmptyState/                    ✅
│   └── PlaceholderBanner/             ✅
├── api/                              # HTTP 封装（王五）
│   ├── http.ts                        ✅
│   ├── auth.ts                        ✅
│   ├── user.ts                        ✅
│   ├── upload.ts                      ✅
│   ├── admin.ts                       ✅
│   ├── learningPath.ts                ✅
│   ├── resources.ts                   ✅
│   └── profile.ts                     ✅
├── store/                            # 状态管理（王五）
│   ├── authStore.ts                   ✅
│   ├── chatStore.ts                   ✅
│   ├── agentTraceStore.ts             ✅
│   ├── artifactStore.ts               ✅
│   ├── pathStore.ts                   ✅
│   └── profileStore.ts                ✅
├── hooks/                            # 自定义 Hook（王五）
│   ├── useWebSocket.ts                ✅ 核心 Hook
│   └── useChatAutoScroll.ts           ✅
├── types/                            # 类型定义（王五，与后端协议对齐）
│   ├── agent-events.ts                ✅
│   ├── artifact.ts                    ✅
│   ├── chat.ts                        ✅
│   ├── multi-card.ts                  ✅
│   ├── media-resource.ts              ✅
│   ├── learning-path.ts               ✅
│   └── profile.ts                     ✅
├── constants/
│   └── agent-labels.ts                ✅ 智能体中文标签
├── styles/
│   └── workspace-theme.css            ✅
└── main.tsx                           ✅ 入口
```

---

## 文档索引

| 文档 | 用途 | 维护人 |
|------|------|--------|
| [README.md](../README.md) | 启动、环境、排错 | 张三 |
| [项目精简开发指南.md](./项目精简开发指南.md) | **本文档** — 开发总纲 | 三人共同 |
| [后端开发进度.md](./后端开发进度.md) | 后端 ✅/🟡/❌ 对应，跟着改 | 张三 |
| [前端功能模块.md](./前端功能模块.md) | 前端页面与组件清单 | 李四+王五 |
| Swagger UI | http://localhost:8080/swagger-ui.html — 接口在线查+测 | 张三维护 |

---

**维护说明**：完成某项任务后，在本文对应复选框打 `[x]`，并同步更新 [后端开发进度.md](./后端开发进度.md) 或 [前端功能模块.md](./前端功能模块.md) 的状态标记。
