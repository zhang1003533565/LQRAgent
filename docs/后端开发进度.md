# LQRAgent 后端开发进度

> 对照开发用。与 `项目精简开发指南.md` 中的 P0～P4 阶段一致。  
> **最后核对**：2026-05-23（以 `backend/src/main/java` 与 SQL 脚本为准）

---

## 状态图例

| 标记 | 含义 |
|------|------|
| ✅ | **已完成**，可正常使用或已落地 |
| 🟡 | **部分完成**，有接口/壳子但未接业务或仅为占位 |
| ❌ | **未开始**，代码中无对应实现 |
| 📋 | **仅数据库**，表已在 SQL 中定义，无 Java 业务层 |

---

## 总览

| 维度 | 已完成 | 部分 | 未做 |
|------|--------|------|------|
| 基础设施 | 9 | 0 | 0 |
| 认证与用户 | 3 | 0 | 0 |
| 管理后台 API | 8 | 0 | 2 |
| 学生业务 API | 2 | 2 | 8+（WS 聊天已在 P0 就绪）|
| 智能体模块（8 个） | 1 | 0 | 7 |
| 数据库表（14 张业务表） | 5 张有 JPA | — | 9 张仅 DDL |

**当前瓶颈**：ai-server 需运行才能完成端到端联调；上传队列 `processTask` 仍为占位。

---

## 一、基础设施（Spring Boot 工程）

| # | 项 | 状态 | 位置 / 备注 |
|---|-----|------|-------------|
| 1 | 应用启动与统一响应 | ✅ | `BackendApplication`、`ApiResponse`、`GlobalExceptionHandler` |
| 2 | MySQL 数据源 + JPA | ✅ | `application.properties` |
| 3 | SQL 初始化（全量表 + 旧库补丁） | ✅ | `backend/src/main/resources/db/init-tables.sql` |
| 4 | JWT 鉴权过滤器 | ✅ | `JwtUtil`、`JwtAuthFilter` |
| 5 | Spring Security + CORS | ✅ | `SecurityConfig`（含 `/ws/**` permitAll，**Handler 未写**） |
| 6 | 定时任务 | ✅ | `SchedulingConfig` |
| 7 | 运行时配置 `sys_config` | ✅ | `SysConfigService`、`AppRuntimeConfig`、`SysConfigSeeder` |
| 8 | 测试账号初始化 | ✅ | `DataInitializer`：`admin` / `student1`，密码 `123456` |
| 9 | ai-server 自动拉起 | ✅ | `AIServerRunner`（`ai-server.auto-start`） |
| 10 | 启动控制台（可选） | ✅ | `ConsoleControlPanel`（`app.console.enabled`） |

---

## 二、认证与用户

| # | 项 | 状态 | API / 备注 |
|---|-----|------|------------|
| 1 | 登录（JWT + 角色 + 跳转路径） | ✅ | `POST /api/auth/login` |
| 2 | 登出（无状态） | ✅ | `POST /api/auth/logout` |
| 3 | 当前用户基础信息 | ✅ | `GET /api/users/me`（无画像字段） |
| 4 | 用户注册 | ✅ | `POST /api/auth/register`，注册成功自动登录 |


---

## 三、管理后台（`AdminController`，角色 ADMIN）

> **说明**：管理端 **面板 UI 由你方自行开发**，不纳入 [前端功能模块.md](./前端功能模块.md)。本节只跟踪 **后端 API** 是否就绪；现有 `frontend/AdminPage` 仅为参考实现，可替换。

| # | 项 | 状态 | API / 备注 |
|---|-----|------|------------|
| 1 | 服务状态概览 | ✅ | `GET /api/admin/status` |
| 2 | 大模型配置读取 | ✅ | `GET /api/admin/model-config` |
| 3 | 大模型配置保存 | ✅ | `PUT /api/admin/model-config` |
| 4 | 大模型连接测试 | ✅ | `POST /api/admin/model-config/test-llm` |
| 5 | 同步配置到 `ai-server/.env` | ✅ | `ModelConfigService` + `AiServerEnvSyncService` |
| 6 | 系统配置 CRUD | ✅ | `GET/PUT/DELETE /api/admin/config` |
| 7 | AI 连通性探测 | ✅ | `POST /api/admin/ai/ping` |
| 8 | 用户列表 | ✅ | `GET /api/admin/users` |
| 9 | 上传任务列表 | ✅ | `GET /api/admin/upload/tasks` |
| 10 | 手动处理一条上传 | ✅ | `POST /api/admin/upload/process` |
| 11 | 智能体调用统计 | ❌ | 计划 `GET /api/admin/agent-stats` |
| 12 | 智能体运行日志列表 | ❌ | 计划 `GET /api/admin/agent-runs` |
| 13 | 某学生画像摘要（只读） | ❌ | 计划 `GET /api/admin/users/{id}/profile-summary` |

---

## 四、学生端 REST API

| # | 项 | 状态 | API / 备注 |
|---|-----|------|------------|
| 1 | 文件上传入队 | ✅ | `POST /api/upload` |
| 2 | 查询本人上传任务 | ✅ | `GET /api/upload/tasks` |
| 3 | 学习路径（占位） | 🟡 | `GET /api/learning-path?goal=`，固定占位节点 |
| 4 | 当前活跃学习路径 | ❌ | 计划 `GET /api/learning-path/current` |
| 5 | 生成/刷新学习路径 | ❌ | 计划 `POST /api/learning-path/generate` |
| 6 | 生成学习资源（文本） | ❌ | 计划 `POST /api/resources/generate`，`LESSON` / `QUIZ` / `CODE_CASE` |
| 7 | 生成示意图（生图） | ❌ | 同上，`type=ILLUSTRATION` → `mediageneration` |
| 8 | 生成讲解视频（生视频） | ❌ | 同上，`type=VIDEO_CLIP`（建议 P4） |
| 9 | 提交答题 | ❌ | 计划 `POST /api/quiz/submit` |
| 10 | 画像摘要 | ❌ | 计划 `GET /api/profile/summary` |
| 11 | WS 协议说明 | ✅ | `GET /api/chat/protocol`，Swagger 可见 |
| 12 | 聊天会话列表 | ✅ | `GET /api/chat/sessions`，按时间倒序，含消息数 |
| 13 | 会话消息历史 | ✅ | `GET /api/chat/sessions/{id}/messages`，按时间正序 |

---

## 五、WebSocket 与聊天

| # | 项 | 状态 | 备注 |
|---|-----|------|------|
| 1 | `/ws/chat` 端点 | ✅ | `ChatWebSocketHandler` + `WebSocketConfig` |
| 2 | WS 握手 JWT 校验 | ✅ | `JwtHandshakeInterceptor` |
| 3 | `AiServerWsProxy`（转发 ai-server） | ✅ | Java 标准 WebSocket 客户端，支持流式回调 |
| 4 | 事件 `chunk` / `done` / `error` | ✅ | Handler 内实现 JSON 事件推送 |
| 5 | 事件 `agent_step` | ✅ | qa_agent start/done/failed 均推送 |
| 6 | 事件 `artifact` / `profile_patch` | ❌ | P1/P2；含 `media_image` / `media_video` / `multi_card` |
| 7 | `ChatSessionService` + `chat_session` 表 | ❌ | 📋 表已有 |
| 8 | `chat_message` 持久化 | ✅ | 每条 USER/AI 消息落库；`GET /api/chat/sessions/{id}/messages` 查询 |

---

## 六、AI 服务客户端（`AiServerClient`）

| # | 方法 / 能力 | 状态 | 备注 |
|---|-------------|------|------|
| 1 | `ping()` | ✅ | `/api/v1/knowledge/health` |
| 2 | `createKnowledgeBase()` | ✅ | HTTP 已实现 |
| 3 | `uploadDocument()` | ❌ | 抛出 `UnsupportedOperationException` |
| 4 | `generateQuestion()` | 🟡 | HTTP 有，**未挂业务流程** |
| 5 | `generateLesson()` | 🟡 | HTTP 有，**未挂业务流程** |
| 6 | WebSocket 客户端 | ✅ | `AiServerWsProxy`：标准 Java WS 客户端，流式回调 |
| 7 | ai-server 事件 → 统一协议映射 | ❌ | 计划 `AiServerEventMapper` |

---

## 七、上传队列（`uploadqueue`）

| # | 项 | 状态 | 备注 |
|---|-----|------|------|
| 1 | 入队 `enqueue` | ✅ | `KbUploadTask` JPA |
| 2 | 按用户 / 全局列表查询 | ✅ | — |
| 3 | 定时轮询 worker | ✅ | `@Scheduled`，间隔可配置 |
| 4 | 手动 `processOnePending` | ✅ | Admin + Console |
| 5 | 状态机 PENDING→PROCESSING→COMPLETED/FAILED | ✅ | — |
| 6 | 调用 ai-server 写入知识库 | ❌ | `processTask` 内 TODO，**当前直接标 COMPLETED** |
| 7 | `content_analyzer` 写 `analysis_result` | ❌ | 📋 SQL 列已有，实体未映射 |
| 8 | 写 `mapped_kp_ids` | ❌ | 📋 SQL 列已有，实体未映射 |

---

## 八、学习路径（`learningpath`）

| # | 项 | 状态 | 备注 |
|---|-----|------|------|
| 1 | `LearningPathController` | ✅ | `GET /api/learning-path` |
| 2 | `LearningPathService` 真逻辑 | ❌ | 返回占位 `kp_placeholder_1` |
| 3 | `KnowledgeGraphService` | ❌ | 包不存在 |
| 4 | 课程 `knowledge_graph.json` 加载入库 | ❌ | 仅有 `knowledge_graph.template.json` |
| 5 | 持久化 `learning_path` / `learning_path_step` | ❌ | 📋 表已有 |
| 6 | 结合 `learner_profile` 调节奏 | ❌ | — |
| 7 | 大模型生成 `plan_description` | ❌ | — |

---

## 九、八个智能体模块（Java 包）

| 智能体 | 计划包名 | 状态 | 说明 |
|--------|----------|------|------|
| 协调智能体 | `orchestrator` | ❌ | 意图路由、调度、推 WS 事件 |
| 内容分析智能体 | `contentanalyzer` | ❌ | 消费上传队列、RAG、写图谱 |
| 路径规划智能体 | `learningpath`（增强） | 🟡 | 仅有占位 Service |
| 资源生成智能体 | `resourcefacade` | ❌ | 聚合 book / question + 媒体类型路由 |
| 媒体生成 | `mediageneration` | ❌ | 生图 / 生视频 API，`resource_item.media_*` |
| 质量评估智能体 | `qualityassessment` | ❌ | 三段校验 + 重试 |
| 学生画像智能体 | `learnerprofile` | ❌ | 📋 `learner_profile` 表已有 |
| 答疑智能体 | `qaagent` | ✅ | P0 已实现，转发 ai-server 流式对话 |
| 效果评估智能体 | `effectassessment` | ❌ | 📋 `study_behavior` 等表已有 |

**可观测性**

| 项 | 状态 | 备注 |
|----|------|------|
| `AgentRunLogService` | ✅ | `observability` 包：Entity + Repo + Service + WS agent_step 推送 |

---

## 十、数据库表 vs Java 层

| 表名 | DDL（SQL 初始化） | JPA Entity | Service / 业务 |
|------|-------------------|------------|----------------|
| `sys_user` | ✅ | ✅ | ✅ |
| `sys_config` | ✅ | ✅ | ✅ |
| `kb_upload_task` | ✅ | ✅ | ✅（未接 AI；缺 v2 列映射） |
| `learner_profile` | ✅ | ❌ | ❌ |
| `knowledge_point` | ✅ | ❌ | ❌ |
| `knowledge_edge` | ✅ | ❌ | ❌ |
| `learning_path` | ✅ | ❌ | ❌ |
| `learning_path_step` | ✅ | ❌ | ❌ |
| `resource_item` | ✅ | ❌ | ❌ |
| `quiz_record` | ✅ | ❌ | ❌ |
| `study_behavior` | ✅ | ❌ | ❌ |
| `path_adjustment_log` | ✅ | ❌ | ❌ |
| `chat_message` | ✅ | ✅ | ✅ |
| `chat_session` | ✅ v2 | ✅ | ✅ |
| `agent_run_log` | ✅ v2 | ✅ | ✅ |

---

## 十一、课程与静态资源

| # | 项 | 状态 | 路径 |
|---|-----|------|------|
| 1 | 课程目录说明 | ✅ | `resources/course/README.md` |
| 2 | 知识图谱模板 | ✅ | `knowledge_graph.template.json` |
| 3 | 正式 `knowledge_graph.json` | ❌ | 需按模板填写并加载 |
| 4 | `outline.json` | ❌ | — |
| 5 | `seed_kb/` 种子文件 | 🟡 | 仅有 README，无实际文件 |
| 6 | 启动时图谱/种子导入 | ❌ | — |

---

## 十二、按项目阶段（P0～P4）

### P0 — 聊天打通（最高优先级）

> **阶段目标**：学生工作台不再「连接中」，能流式对话并在 Timeline 看到智能体步骤。

| 任务 | 说明（一句话） | 状态 |
|------|----------------|------|
| `chat` 包：`ChatWebSocketHandler` | 实现 `/ws/chat` 端点：接收前端消息、校验 JWT、把回复推回浏览器 | ✅ |
| `AiServerWsProxy` | 后端作为中间层连接 ai-server 的 WebSocket，把它的流式输出转成我们的 `chunk`/`done` 协议 | ✅ |
| `ChatSessionService` | 管理 `chat_session` 表：创建会话 ID、关联用户、记录 ai-server 侧 session | ✅ |
| `AgentRunLogService` | 每次智能体调用写入 `agent_run_log`，并向前端推 `agent_step` 事件 | ✅ |
| 默认 `qa_agent` 流式答疑 | P0 先不做复杂路由：用户发消息 → 直接走答疑通道（转发 ai-server 对话） | ✅ |
| **阶段验收** | `student1` 在工作台发消息能逐字显示回复；Timeline 至少有 1～2 步；库里有 `agent_run_log` | 🟡 代码就绪，待联调 ai-server |

### P1 — 路径与资源（MVP 1～4 步）

> **阶段目标**：说出学习目标后出现**真实**学习路径，点节点能生成讲义/题目。

| 任务 | 说明（一句话） | 状态 |
|------|----------------|------|
| `orchestrator` 规则路由 | 协调智能体：根据关键词判断走「路径 / 出题 / 资源 / 默认答疑」，再调用对应模块 | ❌ |
| `KnowledgeGraphService` + 数据入库 | 把 `knowledge_graph.json` 载入 `knowledge_point`/`knowledge_edge`，提供前置依赖与路径搜索 | ❌ |
| `LearningPathService` 真逻辑 + 落库 | 按图谱算节点顺序 + 可选 LLM 写计划说明，结果写入 `learning_path` 与 `step` 表 | ❌ |
| `GET /api/learning-path/current` | 返回该学生当前进行中路径（刷新页面后右侧栏仍能显示） | ❌ |
| `resourcefacade` + `POST /api/resources/generate` | 点击节点生成讲义/题目/代码；`ILLUSTRATION` 走生图 | ❌ |
| `mediageneration` + 媒体存储 | 调生图 API，写 `media_url`；文件落 `storage/media/` 或对象存储 | ❌ |
| WS 推送 `artifact`（路径/资源/媒体） | 下发路径 JSON、资源卡片、`media_image` 等 | ❌ |
| **阶段验收** | 点节点能出讲义或题目；**至少一种知识点能出示意图** | ❌ |

### P2 — 答题与画像（MVP 5～6 步）

> **阶段目标**：做完题能更新掌握情况，左侧画像有变化，路径推荐可跟着调。

| 任务 | 说明（一句话） | 状态 |
|------|----------------|------|
| `learnerprofile` 模块 | 读写 `learner_profile`：掌握度、目标、常见错误等；各智能体按规则更新字段 | ❌ |
| `POST /api/quiz/submit` | 提交答案 → 判分/记录 `quiz_record` → 触发画像与效果评估 | ❌ |
| `GET /api/profile/summary` | 工作台左侧「我的画像」卡片的数据源 | ❌ |
| `qualityassessment` | 文本资源检索/LLM 校验；**媒体**校验尺寸/时长/敏感词 | ❌ |
| WS `profile_patch` | 答题或聊天后通过 WS 推送画像增量，前端无需整页刷新 | ❌ |
| `chat_message` MULTI_CARD | 答疑回复可含图片卡片 JSON（`content_type=MULTI_CARD`） | ❌ |
| **阶段验收** | 提交一道题后画像数字或文案变化；路径下一步推荐有依据 | ❌ |

### P3 — 上传进库 + 管理监控（MVP 7 步）

> **阶段目标**：上传的资料真正进入知识库；管理员能看到智能体运行情况。

| 任务 | 说明（一句话） | 状态 |
|------|----------------|------|
| `uploadDocument` 实现 | 补全 `AiServerClient.uploadDocument`：把队列里的文件发给 ai-server 做 RAG 入库 | ❌ |
| `contentanalyzer` + 写分析结果 | 上传处理完成后解析文档、映射知识点，写 `analysis_result` 与 `mapped_kp_ids` | ❌ |
| `KbUploadTask` 实体增加 v2 字段 | JPA 实体对齐 SQL 中的 `analysis_result`、`mapped_kp_ids`，Admin/学生能读到 | ❌ |
| `effectassessment` + 行为/路径调整日志 | 记录停留、错题等行为到 `study_behavior`，按规则改路径并写 `path_adjustment_log` | ❌ |
| Admin `agent-stats` / `agent-runs` | 管理后台 API：统计各智能体调用次数/失败率、分页查最近运行日志 | ❌ |
| **阶段验收** | 上传 PDF 后队列 COMPLETED 且库里有映射知识点；Admin 概览能看到 Agent 统计 | ❌ |

### P4 — 加分项（含生视频）

> **阶段目标**：体验与可维护性提升；**生视频**、完整会话回放放本阶段。

| 任务 | 说明（一句话） | 状态 |
|------|----------------|------|
| `VIDEO_CLIP` 生视频 | `mediageneration` 接生视频 API，写 `resource_item`，WS 推 `media_video` | ❌ |
| 管理端媒体 API 配置 | `media.image.*` / `media.video.*` 写入 `sys_config`，Admin 可配 | ❌ |
| LLM 意图分类（替代纯规则） | 用模型判断「要路径 / 要题 / 要配图 / 要视频 / 闲聊」 | ❌ |
| 路径动态调整规则 | 根据 P2 行为数据自动插复习节点、补代码案例等 | ❌ |
| `chat_message` 全量持久化 | 含 `MULTI_CARD` 多模态消息的历史回放 | ❌ |
| Redis Streams 异步总线 | 同步调用 → Redis 发布/订阅解耦，支持智能体并行执行再聚合 | ❌ |
| Nginx API 网关 | 生产部署 Nginx 反代静态资源 + 路由 /api /ws，当前开发模式直连 | ❌ |
| 内容安全审核 | 对接讯飞/百度审核 API，对 AI 生成内容和用户上传文档做合规检查 | ❌ |

---

## 十三、原「七阶段」对照（历史规划）

| 阶段 | 内容 | 后端状态 |
|------|------|----------|
| 1 | 固定课程数据 | 🟡 模板有，无加载代码 |
| 2 | 登录 + 角色 | ✅ 完成 |
| 3 | WebSocket 封装 | ✅ P0 完成 |
| 4 | `AiServerClient` 壳子 | 🟡 HTTP 部分完成 |
| 5 | 聊天 / 知识库 / 题目 / 答疑 | 🟡 仅 ping + 未串联的 generate |
| 6 | 上传队列 + worker | 🟡 队列有，未接 AI |
| 7 | 最小学习路径 | 🟡 占位 API |

---

## 十四、详细开发任务清单（勾选跟踪，与精简开发指南一致）

> 每个任务对应 [项目精简开发指南](./项目精简开发指南.md) 中后端同名任务。
> 完成后将 `[ ]` 改为 `[x]`，同步更新文首「最后核对」日期。

### P0 — 聊天打通（目标：不再「连接中」，能流式回复）

```
[x] P0-1  ChatWebSocketHandler              新建 chat/handler/ChatWebSocketHandler.java
                                             实现 /ws/chat 端点：JWT 握手、接收消息、
                                             管理 sessions map、转交 QaAgent
[x] P0-2  AiServerWsProxy                   新建 chat/proxy/AiServerWsProxy.java
                                             连接 ai-server WS、接收流式响应、
                                             转换为 chunk/done/error 事件推给前端
[x] P0-3  QaAgentService                    新建 qaagent/QaAgentService.java
                                             默认答疑通道：所有消息直接转发 ai-server
[x] P0-4  AgentRunLogService                新建 observability/ 包
                                             写 agent_run_log 表、推 agent_step 事件
                                             Entity + Repository + Service
[x] P0-5  ChatSessionService                新建 chat/ 下 Entity + Repo + Service
                                             管理 chat_session 生命周期
```

### P1 — 路径与资源（目标：真路径 + 生图 + 5 种资源）

```
[ ] P1-1  knowledge_graph.json 填写         填写至少 5-6 章、~30 个知识点的完整图谱
[ ] P1-2  KnowledgeGraphService              新建 knowledgegraph/ 包
                                             启动加载 JSON 入库、提供前置查询/BFS寻路
[ ] P1-3  LearningPathService 真逻辑         改造 learningpath/service/
                                             关键词匹配目标节点 → BFS 算路径 →
                                             落库 learning_path/step → WS 推 artifact
[ ] P1-4  GET /api/learning-path/current     返回当前活跃路径（刷新恢复）
[ ] P1-5  OrchestratorService               新建 orchestrator/ 包
                                             关键词规则路由（至少 6 种意图）
                                             调度对应智能体、记录 agent_run_log
[ ] P1-6  ResourceFacadeService              新建 resourcefacade/ 包
                                             统一入口 generate(kpId, type)
                                             LESSON→ai-server book, QUIZ→ai-server question
                                             CODE_CASE→ai-server, ILLUSTRATION→MediaGen
[ ] P1-7  POST /api/resources/generate      新建 controller + 接口
[ ] P1-8  MediaGenerationService            新建 mediageneration/ 包
                                             拼绘图 prompt → 调生图 API → 保存文件
                                             → 写 resource_item → WS 推 media_image
[ ] P1-9  媒体文件访问 API                   GET /api/media/{id} 返回图片/视频文件
```

### P2 — 答题与画像（目标：答题改画像、MULTI_CARD）

```
[ ] P2-1  LearnerProfileService             新建 learnerprofile/ 包
                                             Entity + Repo + Service
                                             GET /api/profile/summary (≥6 维)
[ ] P2-2  QuizController + POST /api/quiz/submit 新建接口
                                             提交答案→判分→写 quiz_record→更新画像
[ ] P2-3  QualityAssessmentService          新建 qualityassessment/ 包
                                             文本非空校验、媒体尺寸校验、失败重试
[ ] P2-4  WS profile_patch 推送             答题/聊天后推送画像增量给前端
[ ] P2-5  WS artifact multi_card 推送       答疑回复含图片卡片 JSON（contentType=MULTI_CARD）
```

### P3 — 上传分析 + 管理监控

```
[ ] P3-1  uploadDocument 实现               补全 AiServerClient.uploadDocument
                                             调 ai-server 文档上传 API
[ ] P3-2  KbUploadTask 实体 v2 字段         增加 analysisResult、mappedKpIds 字段
[ ] P3-3  ContentAnalyzerService            新建 contentanalyzer/ 包
                                             文档→LLM提取知识点→写 mapped_kp_ids
[ ] P3-4  UploadQueueService 接 AI          改造 processTask()，接 content_analyzer
[ ] P3-5  EffectAssessmentService           新建 effectassessment/ 包
                                             记录 study_behavior、写 path_adjustment_log
[ ] P3-6  Admin GET /api/admin/agent-stats  各 agent 调用次数、成功率、平均耗时
[ ] P3-7  Admin GET /api/admin/agent-runs   最近 agent_run_log 分页
```

### P4 — 加分项

```
[ ] P4-1  MediaGeneration → VIDEO_CLIP      接生视频 API（可灵/Runway 等）
[ ] P4-2  管理端媒体 API 配置                sys_config 增加 media.image.* / media.video.*
[ ] P4-3  LLM 意图分类替代规则路由            用大模型判断意图，替换关键词匹配
[ ] P4-4  路径动态调整（错题→复习节点）      根据错题率自动插入复习知识点
[ ] P4-5  chat_message 全量持久化            含 MULTI_CARD 多模态消息历史回放
[ ] P4-6  Redis Streams 异步总线             同步调用→Redis 发布/订阅解耦，支持智能体并行执行
[ ] P4-7  Nginx API 网关                    生产部署 Nginx 反代，开发模式不动
[ ] P4-8  内容安全审核（可选）               对接第三方审核 API，AI 内容合规检查
```

### 建议的每周推进节奏

| 周次 | 主要冲刺 | 产物 |
|------|----------|------|
| Week 1 | P0-1 ~ P0-5 | WS 连通、流式回复、AgentTimeline 有步骤 |
| Week 2 | P1-1 ~ P1-5 | 知识图谱入库、真实路径、Orchestrator 路由 |
| Week 3 | P1-6 ~ P1-9 | 资源生成、生图、前端可看讲义+示意图 |
| Week 4 | P2-1 ~ P2-5 | 答题+画像+质检、MULTI_CARD |
| Week 5 | P3-1 ~ P3-7 | 上传真入库、Admin 监控 |
| Week 6 | P4-1 ~ P4-5 | 加分项 + 联调打磨 |

---

## 十五、相关文档

- [文档索引](./文档索引.md)  
- [README](../README.md) — 启动、环境、排错  
- [前端功能模块](./前端功能模块.md) — 前端页面与组件清单  
- [项目精简开发指南](./项目精简开发指南.md) — 架构、事件协议、P0～P4  
- [init-tables.sql](../backend/src/main/resources/db/init-tables.sql) — 数据库 DDL  
- **接口文档**：启动后端后访问 http://localhost:8080/swagger-ui.html

---

**维护说明**：完成某项后，将表中 `❌`/`🟡` 改为 `✅`，并更新文首「最后核对」日期。

**多模态**：详见 [项目精简开发指南 §8.1](./项目精简开发指南.md)。
