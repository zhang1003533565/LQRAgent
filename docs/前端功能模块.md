# LQRAgent 前端功能模块说明（学生工作台）

> **给谁看**：负责 **学生端** 前端的同学。  
> **写什么**：学生工作台要做什么页面与组件（不写接口字段，见 [前端可对接API](./前端可对接API.md)）。  
> **不含**：管理后台 UI —— 由你方自行开发，能力清单见 [后端开发进度 §三](./后端开发进度.md)。  
> **最后更新**：2026-05-22

---

## 一、产品边界

| 本文档覆盖（学生端） | 不在本文档（另见后端进度 / 自研 Admin） |
|--------------------|----------------------------------------|
| 登录后进入 `/workspace` 的学习体验 | `/admin` 管理面板布局与 Tab |
| 聊天、路径、资源、答题、画像、上传 | 模型配置、队列运维、用户管理等后台界面 |
| 多模态资源展示（图 / 视频） | 用户自助注册 |

| 明确不做（相对 DeepTutor 开源 UI） |
|-----------------------------------|
| 不嵌入 `ai-server/web` |
| 不做协作写作、图书编辑器、演练场 |
| 浏览器不直连 `localhost:8001` |

---

## 二、页面与路由（仅学生端）

```
/login                          登录
/workspace                      工作台 · 聊天（默认）
/workspace/upload               工作台 · 上传资料
/workspace/profile              工作台 · 学习画像
```

管理员登录后跳转 `/admin` 的界面 **不在本文档范围**；现有 `frontend/src/pages/AdminPage.tsx` 仅为占位/参考，可按你方后台方案替换或移除。

| 路由 | 模块编号 |
|------|----------|
| `/login` | M1 |
| `/workspace/*` | M2～M8 |

---

## 三、工作台布局（左右两栏，对齐 DeepTutor）

**壳子**：`flex` 左窄侧栏 + 右 `flex-1` 主区（`features/workspace/shell/WorkspaceShell`）。

- **左侧**：品牌、用户卡片、路由导航（聊天 / 上传 / 画像）。
- **右侧主区**：按子路由切换整页；**不使用第三栏**。

**聊天页主区内 Tab**（`context/MainTabBar`）：对话 | 学习路径 | 资源 | 练习。

```
┌──────────┬────────────────────────────────────┐
│ 侧栏      │ 主区                                │
│ · 用户    │  [对话][路径][资源][练习]  ← 仅聊天页 │
│ · 导航    │  当前 Tab 或子路由页面               │
└──────────┴────────────────────────────────────┘
```

| 子模块 | 功能 | 状态 |
|--------|------|------|
| 工作台壳子 | 路由出口 + 可调整布局 | ✅ `WorkspacePage.tsx` |
| 功能导航 | 聊天、上传、画像入口 | ✅ |
| 子路由 | `index` 聊天；`upload`；`profile` | 🟡 profile 占位 |

---

## 四、功能模块清单

图例：**✅ 已有** · **🟡 部分** · **❌ 待开发** · **⏳ 后期（P4）**

### M0 · 应用壳与公共能力

| 子模块 | 功能 | 状态 |
|--------|------|------|
| 路由与鉴权 | `ProtectedRoute`、学生进 `/workspace` | ✅ |
| `authStore` + `http` | token、401 跳登录 | ✅ |

---

### M1 · 登录 `/login`

| 功能 | 状态 |
|------|------|
| 账号密码登录 | ✅ |
| 学生 → `/workspace`；管理员 → `/admin`（后台自研） | ✅ |
| 注册 / 找回密码 | ❌ 不做 |

---

### M3 · 聊天学习（工作台默认）

| 子模块 | 功能 | 状态 |
|--------|------|------|
| 消息列表 + 输入 | | ✅ `ChatPanel` |
| 流式 Markdown | | ✅ `StreamingMessage` |
| WebSocket | chunk / done / error | 🟡 待后端 `/ws/chat` |
| **智能体步骤条** | `agent_step` | ❌ `AgentTimeline` |
| **多模态消息** | `MULTI_CARD` 图文混排 | ❌ |
| **artifact 卡片** | 路径/示意图等进聊天或辅区 | ❌ |
| 多会话列表 | | ⏳ P4；MVP 单会话即可 |

---

### M4 · 学习路径

| 功能 | 状态 |
|------|------|
| 输入目标、展示节点列表 | ✅ `LearningPathPanel` |
| 点击节点触发资源生成 | ❌ |
| 与聊天目标联动 | ❌ 可选 |
| 刷新后恢复当前路径 | ❌ |

放置位置：辅区常驻，或聊天页内折叠面板，由布局方案决定。

---

### M5 · 学习资源展示

| 类型 | 状态 |
|------|------|
| 讲义 / 题目 / 代码 | ❌ |
| 示意图（生图） | ❌ P1 |
| 短视频（生视频） | ❌ P4 |

| 子模块 | 状态 |
|--------|------|
| `ResourceTabs` + 文本渲染 | ❌ |
| `MediaPreview`（图/视频） | ❌ |
| `api/resources.ts` | ❌ |

可与 M4 同区（Tab），或在节点点击后以抽屉/弹层呈现。

---

### M6 · 答题

| 功能 | 状态 |
|------|------|
| 展示题、提交、反馈 | ❌ `QuizPanel` |
| 提交后更新画像摘要 | ❌ |

可并入 M5「题目」Tab，或独立小块 UI。

---

### M7 · 上传 `/workspace/upload`

| 功能 | 状态 |
|------|------|
| 上传文件、任务列表、轮询 | ✅ |
| 完成后展示映射知识点 / 分析摘要 | ❌ P3 |

---

### M8 · 学习画像

| 功能 | 状态 |
|------|------|
| 全局摘要卡（昵称、退出；后续加掌握度等） | 🟡 `ProfileSummaryCard` |
| `/workspace/profile` 详情页 | ❌ |

---

## 五、状态管理与 API（学生端）

| 项 | 状态 |
|----|------|
| `chatStore`、`useWebSocket` | ✅ / 🟡 |
| `agentTraceStore`、`artifactStore`、`pathStore` | ❌ |
| `api/auth` `user` `upload` `learningPath` | ✅ |
| `api/resources`、`api/profile` | ❌ |
| `types/agent-events.ts`、`media-resource.ts` | ❌ |

`api/admin.ts` 留给自研管理后台使用，**不计入**学生端模块验收。

---

## 六、与后端交互（学生端）

| 模块 | 方式 |
|------|------|
| M3 聊天、步骤、artifact | WebSocket |
| M4～M8 | HTTP（上传可轮询） |

---

## 七、按阶段交付（仅学生端）

| 阶段 | 交付 | 验收 |
|------|------|------|
| **P0** | M3 聊天 + `AgentTimeline` | 能流式回复、有智能体步骤 |
| **P1** | M4 真路径 + M5（含示意图） | 点节点能看内容与配图 |
| **P2** | M6 答题 + M8 画像 + 聊天多模态卡片 | 答题后画像有变化 |
| **P3** | M7 上传分析结果展示 | 上传后能看到映射知识点 |
| **P4** | M5 视频、多会话等 | 短视频与体验加分 |

管理端能力（配置、队列、Agent 监控）按 [后端开发进度](./后端开发进度.md) 与你方 Admin 面板自行对齐。

---

## 八、建议目录（学生端为主）

```
frontend/src/
├── app/                    # App、ProtectedRoute
├── pages/                  # LoginPage、WorkspacePage、AdminPage
├── features/
│   ├── workspace/          # 学生工作台
│   └── admin/              # 管理面板组件
├── components/ui/          # Badge、EmptyState 等（每组件子目录 + index）
├── api/ store/ hooks/ types/ constants/ styles/
└── main.tsx
```

详见 `frontend/src/README.md`。

---

## 九、相关文档

| 文档 | 用途 |
|------|------|
| [前端可对接API](./前端可对接API.md) | 接口 |
| [后端开发进度](./后端开发进度.md) | 后端 + **管理后台 API / 自研面板** |
| [项目精简开发指南](./项目精简开发指南.md) | 架构、多模态、WS 协议 |

**维护**：只更新学生端模块状态；管理后台 UI 请在后端进度或你自己的 Admin 文档中维护。
